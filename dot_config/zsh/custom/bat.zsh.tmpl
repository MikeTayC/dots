{{ if (index (default dict .tools) "bat") }}
export MANPAGER="sh -c 'col -bx | bat -l man -p'"

# Replace cat with bat
alias ccat='/bin/cat'
{{- if eq .chezmoi.os "darwin" }}
alias cat='bat --paging=never'
{{- else }}
alias cat='batcat --paging=never -p'
{{- end }}
{{- end }}
