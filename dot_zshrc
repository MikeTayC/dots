# Set options
setopt histignorealldups sharehistory
bindkey -e

HISTSIZE=1000
SAVEHIST=1000
HISTFILE=~/.zsh_history

# Completion
autoload -Uz compinit
compinit

# Colors
eval "$(dircolors -b)"

# Starship (LAST LINE)
eval "$(starship init zsh)"
